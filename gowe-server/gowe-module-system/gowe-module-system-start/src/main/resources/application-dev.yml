server:
  port: 8080
# spring
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/gowe?characterEncoding=UTF-8&useUnicode=true&useSSL=false&tinyInt1isBit=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis-plus
mybatis-plus:
  mapper-locations: classpath*:com/gowe/modules/**/xml/*Mapper.xml
  global-config:
    # 关闭mp的banner
    banner: false